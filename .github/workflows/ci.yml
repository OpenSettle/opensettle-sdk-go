name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        go: ["1.22.x", "1.23.x"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          cache: true
      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "gofmt found unformatted files:" >&2
            echo "$unformatted" >&2
            exit 1
          fi
      - name: go mod tidy is clean
        run: |
          go mod tidy
          # Use porcelain status so a zero-dependency module (no go.sum) doesn't
          # false-fail: `git diff -- go.sum` errors when the path doesn't exist.
          if [ -n "$(git status --porcelain -- go.mod go.sum)" ]; then
            echo "go.mod / go.sum drifted from 'go mod tidy' — please run it locally and commit the result" >&2
            git --no-pager diff -- go.mod go.sum
            exit 1
          fi
      - name: go vet
        run: go vet ./...
      - name: go test
        run: go test -race -count=1 ./...
